diff -rup1 ubs_org/board/sunxi/dram_a10s_olinuxino_m.c ubs_patched/board/sunxi/dram_a10s_olinuxino_m.c
--- ubs_org/board/sunxi/dram_a10s_olinuxino_m.c	2014-04-09 18:50:55.026407527 +0200
+++ ubs_patched/board/sunxi/dram_a10s_olinuxino_m.c	2014-04-09 18:47:25.510414544 +0200
@@ -6,4 +6,8 @@
 static struct dram_para dram_para = {
-	.clock = 432,
-	.type = 3,
+    /* RED-Brick-OPT: Increasing RAM clock form 432MHz to 480MHz*/
+	/*
+    .clock = 432,
+	*/
+    .clock = 480,
+    .type = 3,
 	.rank_num = 1,
@@ -12,4 +16,8 @@ static struct dram_para dram_para = {
 	.bus_width = 16,
+    /* RED-Brick-OPT: Decreasing CAS delay from 9 to 6*/
+	/*
 	.cas = 9,
-	.zq = 123,
+	*/
+    .cas = 6,
+    .zq = 123,
 	.odt_en = 0,
diff -rup1 ubs_org/boards.cfg ubs_patched/boards.cfg
--- ubs_org/boards.cfg	2014-04-09 18:50:55.042407527 +0200
+++ ubs_patched/boards.cfg	2014-04-09 18:46:54.350415587 +0200
@@ -370,3 +370,5 @@ Active  arm         armv7          sunxi
 Active  arm         armv7          sunxi       -               sunxi               A10-OLinuXino-Lime                   sun4i:A10_OLINUXINO_L,STATUSLED=226,SPL,SUNXI_EMAC                                                                                -
-Active  arm         armv7          sunxi       -               sunxi               A10s-OLinuXino-M                     sun5i:A10S_OLINUXINO_M,STATUSLED=131,AXP152_POWER,CONS_INDEX=1,SPL,SUNXI_EMAC                                                     -
+# RED-Brick-OPT: Disable LED, power and ethernet support
+#Active  arm         armv7          sunxi       -               sunxi               A10s-OLinuXino-M                     sun5i:A10S_OLINUXINO_M,STATUSLED=131,AXP152_POWER,CONS_INDEX=1,SPL,SUNXI_EMAC                                                     -
+Active  arm         armv7          sunxi       -               sunxi               A10s-OLinuXino-M                     sun5i:A10S_OLINUXINO_M,CONS_INDEX=1,SPL                                                     -
 Active  arm         armv7          sunxi       -               sunxi               A10s-OLinuXino-M_FEL                 sun5i:A10S_OLINUXINO_M,STATUSLED=131,AXP152_POWER,CONS_INDEX=1,SPL_FEL,SUNXI_EMAC                                                 -
diff -rup1 ubs_org/common/spl/spl.c ubs_patched/common/spl/spl.c
--- ubs_org/common/spl/spl.c	2014-04-09 18:50:55.062407526 +0200
+++ ubs_patched/common/spl/spl.c	2014-04-09 18:47:11.214415023 +0200
@@ -47,5 +47,10 @@ __weak int spl_start_uboot(void)
 {
-	puts("SPL: Please implement spl_start_uboot() for your board\n");
+    /* RED-Brick-OPT: Turning off following console outputs and returning
+       zero so SPL won't start U-Boot */
+	/*
+    puts("SPL: Please implement spl_start_uboot() for your board\n");
 	puts("SPL: Direct Linux boot not active!\n");
-	return 1;
+    return 1;
+	*/
+    return 0;
 }
diff -rup1 ubs_org/common/spl/spl_mmc.c ubs_patched/common/spl/spl_mmc.c
--- ubs_org/common/spl/spl_mmc.c	2014-04-09 18:50:55.062407526 +0200
+++ ubs_patched/common/spl/spl_mmc.c	2014-04-09 18:47:18.758414770 +0200
@@ -36,11 +36,23 @@ static int mmc_load_image_raw(struct mmc
 
-	spl_parse_image_header(header);
+    /* RED-Brick-OPT: Reading script.bin from sector 80 and saving to 
+       RAM location 0x43000000 (1124073472).
+       Size of script.bin is 144 sectors (72kB) */
+	err = mmc->block_dev.block_read(0, 80, 144, (void *)1124073472);
 
-	/* convert size to sectors - round up */
-	image_size_sectors = (spl_image.size + mmc->read_bl_len - 1) /
-				mmc->read_bl_len;
+    /* RED-Brick-OPT: Proceed to load uImage only if boot script is loaded */
+    if(err != 0) {
+        spl_parse_image_header(header);
 
-	/* Read the header too to avoid extra memcpy */
-	err = mmc->block_dev.block_read(0, sector, image_size_sectors,
-					(void *)spl_image.load_addr);
+        /* convert size to sectors - round up */
+        image_size_sectors = (spl_image.size + mmc->read_bl_len - 1) /
+                              mmc->read_bl_len;
+
+        /* Read the header too to avoid extra memcpy */
+        err = mmc->block_dev.block_read(0, sector, image_size_sectors,
+                                       (void *)spl_image.load_addr);
+    }
+    else {
+        printf("spl: can't load script.bin\n");
+        return -1;
+    }
 
diff -rup1 ubs_org/include/configs/sunxi-common.h ubs_patched/include/configs/sunxi-common.h
--- ubs_org/include/configs/sunxi-common.h	2014-04-09 18:50:55.418407514 +0200
+++ ubs_patched/include/configs/sunxi-common.h	2014-04-09 18:47:01.434415349 +0200
@@ -270,3 +270,6 @@
 #define CONFIG_SPL_LIBGENERIC_SUPPORT
+/* RED-Brick-OPT: Turning off SPL messages */
+/*
 #define CONFIG_SPL_DISPLAY_PRINT
+*/
 
@@ -322,3 +325,7 @@
 #define CONFIG_SYS_MMCSD_RAW_MODE_ARGS_SECTORS  256
+/* RED-Brick_OPT: Changing kernel sector */
+/*
 #define CONFIG_SYS_MMCSD_RAW_MODE_KERNEL_SECTOR 1600
+*/
+#define CONFIG_SYS_MMCSD_RAW_MODE_KERNEL_SECTOR 224
 #endif
@@ -332,3 +339,6 @@
 #ifndef CONFIG_SUN6I
+/* RED-Brick-OPT: Disable I2C support for SPL */
+/*
 #define CONFIG_SPL_I2C_SUPPORT
+*/
 #endif
@@ -350,3 +360,5 @@
 
+/* RED-Brick-OPT: Disable power support */
 /* PMU */
+/*
 #if !defined CONFIG_AXP152_POWER && !defined CONFIG_AXP221_POWER && !defined CONFIG_NO_AXP
@@ -357,2 +369,3 @@
 #endif
+*/
 
